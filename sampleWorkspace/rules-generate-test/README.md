# Generate Config Files Test Workspace

This workspace is used for testing the `generateConfigs` command.

## Configuration

- **Adapter**: Cursor (enabled)
- **Source**: ai-rules repository
- **Purpose**: Test config file generation

## Test Scenario

1. Sync rules from the configured source
2. Select rules
3. Generate config files
4. Verify `.cursorrules` file is created with correct content
