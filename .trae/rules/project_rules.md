<!-- TURBO-AI-RULES:BEGIN -->
<!-- Generated by Turbo AI Rules at 2025-12-28T06:00:05.015Z -->
<!-- Total rules: 3 -->

# AI Coding Rules for Trae

> This file is automatically generated by Turbo AI Rules extension.
## Table of Contents

- [VSCode 扩展开发通用规范](#vscode-扩展开发通用规范)
- [项目规范](#项目规范)
- [Turbo AI Rules 项目开发规范](#turbo-ai-rules-项目开发规范)

---

<!-- BEGIN_SOURCE source="ai-rules" count="1" -->

<!-- BEGIN_RULE source="ai-rules" id="260-vscode-extension" priority="medium" -->
# VSCode 扩展开发通用规范
| Property | Value |
|----------|-------|
| id | 260-vscode-extension |
| title | VSCode 扩展开发通用规范 |
| description | VSCode 扩展开发核心架构决策和关键模式 |
| globs | `**/extension.ts`, `**/src/**/*.ts` |
| priority | medium |
| tags | `vscode`, `extension`, `typescript`, `architecture` |
| version | 2.0.0 |
| author | ygqygq2 |
| lastUpdated | "2025-12-27T00:00:00.000Z" |

## 适用场景

VSCode 扩展项目，TypeScript 严格模式开发

## 核心内容

按功能分模块（commands/services/providers/utils），分层清晰，依赖注入优于单例

## 关键原则

✅ 功能分层：commands（命令）→ services（业务）→ core（算法）→ utils（工具）  
✅ TypeScript strict mode，unknown + 类型守卫代替 any  
✅ activate 注册资源，deactivate 清理，统一 context.subscriptions  
✅ 异步操作 try-catch，错误必须 showErrorMessage 或日志记录  
✅ Provider 用 EventEmitter 触发 UI 刷新

## 目录结构（建议）

```
src/
├── extension.ts          # activate/deactivate
├── commands/             # 命令实现（每个命令一个文件）
├── providers/            # TreeDataProvider/WebviewProvider 等
├── services/             # 业务逻辑
├── core/                 # 核心算法/解析器
├── utils/                # 工具函数
├── types/                # 类型定义
└── constants.ts          # 命令 ID/配置键常量
```

## 关键决策点

### 生命周期管理

**为什么:** 集中注册避免泄漏，清理资源防止内存溢出

```typescript
export function activate(context: vscode.ExtensionContext) {
  // 所有 Disposable 必须 push 到 context.subscriptions
  context.subscriptions.push(
    vscode.commands.registerCommand('ext.cmd', handler),
    vscode.window.createTreeView('view', { treeDataProvider }),
    vscode.workspace.onDidChangeConfiguration(onConfigChange)
  );
}

export function deactivate() {
  // 清理全局资源（Logger、Timer、Cache 等）
}
```

### 命令组织模式

**为什么:** 每个命令独立文件，职责单一，易测试

```typescript
// commands/sync-rules.ts
export async function syncRulesCommand() {
  try {
    await service.sync();
  } catch (error) {
    handleError(error, 'Sync failed');
  }
}

// commands/index.ts - 统一导出
export * from './sync-rules';
```

### Provider 响应式更新

**为什么:** UI 数据变更需主动触发刷新

```typescript
class MyTreeProvider implements vscode.TreeDataProvider<Item> {
  private _onDidChange = new vscode.EventEmitter<Item | undefined>();
  readonly onDidChangeTreeData = this._onDidChange.event;

  refresh() { this._onDidChange.fire(undefined); }
  getTreeItem(el: Item) { return el; }
  getChildren(el?: Item): Thenable<Item[]> { /*...*/ }
}
```

### 配置管理封装

**为什么:** 类型安全 + 默认值集中管理

```typescript
class ConfigManager {
  private static SECTION = 'myExt';
  
  static get<T>(key: string, defaultValue: T): T {
    return vscode.workspace.getConfiguration(this.SECTION).get(key, defaultValue);
  }
  
  static update(key: string, value: unknown, scope = vscode.ConfigurationTarget.Workspace) {
    return vscode.workspace.getConfiguration(this.SECTION).update(key, value, scope);
  }
}
```

### 类型守卫处理 unknown

**为什么:** 避免 any，保持类型安全

```typescript
interface Config { enabled: boolean; path: string; }

function isConfig(obj: unknown): obj is Config {
  return typeof obj === 'object' && obj !== null 
    && 'enabled' in obj && 'path' in obj;
}

// 使用
const data: unknown = getSomeData();
if (isConfig(data)) {
  console.log(data.enabled); // 类型安全
}
```

## 命名约定

- 文件: `kebab-case.ts`
- 类/接口: `PascalCase`
- 函数/变量: `camelCase`
- 常量: `UPPER_SNAKE_CASE`
- 私有成员: `_prefix`

## package.json 核心配置

```json
{
  "activationEvents": ["onStartupFinished"],  // 懒加载优先
  "main": "./out/extension.js",
  "contributes": {
    "commands": [{ "command": "ext.cmd", "title": "My Cmd" }],
    "configuration": { "properties": { "ext.key": { "type": "boolean" } } }
  }
}
```

## 关键 API 使用场景

| API | 场景 | 说明 |
|-----|------|------|
| `vscode.window.showQuickPick` | 列表选择 | 支持多选、搜索 |
| `vscode.window.showInputBox` | 用户输入 | 必须验证输入 |
| `vscode.window.withProgress` | 长任务 | 显示进度 + 支持取消 |
| `context.globalState` | 全局状态 | 跨工作区持久化 |
| `context.workspaceState` | 工作区状态 | 当前工作区持久化 |
| `context.secrets` | 敏感信息 | Token/密码安全存储 |
<!-- END_RULE -->

<!-- END_SOURCE source="ai-rules" -->

<!-- USER-RULES:BEGIN -->
<!-- BEGIN_SOURCE source="user-rules" count="2" -->

<!-- BEGIN_RULE source="user-rules" id="80001" priority="high" -->
# 1. 日志路径显示规范
| Property | Value |
|----------|-------|
| id | 80001 |
| title | 项目规范 |
| description | 日志中的路径应显示相对路径而非绝对路径 |
| globs | `**/*.ts`, `**/*.js` |
| priority | high |
| tags | `project-rules` |
| version | 1.0.0 |
| author | ygqygq2 |
| lastUpdated | "2025-12-27T00:00:00.000Z" |

所有日志输出中涉及路径展示时，除非明确说明，默认都应该展示相对路径（相对于工作区根目录），即使代码中使用的是绝对路径。

# 2. git commit 时 message 精简
- 修改好代码，不要自己 commit，commit 前用户确认，不要自己随便就 commit 了。
- commit message 只写必要信息，避免冗长描述，特别是别加什么测试了啥。
<!-- END_RULE -->

---

<!-- BEGIN_RULE source="user-rules" id="80002" priority="high" -->
# Turbo AI Rules 项目开发规范
| Property | Value |
|----------|-------|
| id | 80002 |
| title | Turbo AI Rules 项目开发规范 |
| description | 从 Git 仓库管理 AI 规则的 VSCode 扩展项目特定规范 |
| globs | `**/*.ts`, `**/*.tsx`, `**/*.md` |
| priority | high |
| tags | `vscode-extension`, `typescript`, `vitest`, `webview`, `superdesign` |
| version | 1.0.0 |
| author | ygqygq2 |
| lastUpdated | "2025-12-27T00:00:00.000Z" |

## 适用场景

Turbo AI Rules 扩展开发，管理外部 Git 仓库的 AI 编码规则并生成多工具配置

## 核心内容

分层架构 + 错误码系统 + 统一日志 + Vitest 单元测试 + SuperDesign Webview

## 关键原则

✅ **每次任务前建 todolist**  
✅ 严格模式，避免 any（必须时注释原因）  
✅ 文件 <500 行（硬性上限 700 行）  
✅ 函数 <50 行，单一职责  
✅ 独立任务用 Promise.all 并行  
✅ 设计-代码-文档三位一体同步

## 错误码系统（强制）

### 格式

`TAI-{分类}{序号}`，挂载在 `error.code` 或日志上下文

### 分类表

| 分类 | 范围 | 说明 |
|------|------|------|
| 配置类 | TAI-100x | 缺失/格式错误/范围越界 |
| Git 类 | TAI-200x | 克隆/拉取/鉴权/分支异常 |
| 解析类 | TAI-300x | frontmatter/MDC/语义校验 |
| 生成类 | TAI-400x | 文件生成/覆盖/写入失败 |
| 系统类 | TAI-500x | IO/权限/路径越界/资源不足 |

### 用户提示

错误信息必须包含 "问题 + 建议"

```typescript
throw new Error('TAI-2001: 克隆仓库失败，请检查仓库 URL 或网络代理设置');
```

详细列表见 `docs/development/01-design.md`

## 日志规范（强制）

### 统一日志库

`@ygqygq2/vscode-log`（必须使用）

```typescript
import { Logger } from '@ygqygq2/vscode-log';

// extension.ts 初始化
const logger = new Logger('TurboAiRules');

// 结构化输出
logger.info('Sync start', { sourceId, count });
logger.error('Sync failed', { error, sourceId });
```

### 等级选择

debug/info/warn/error，按场景选择最小可用等级

### 安全要求

**禁止输出** Token、密码、邮箱等敏感信息

## 模块化拆分规范

### 拆分阈值

- 硬性上限: 700 行
- 建议上限: 500 行
- 函数/方法: <50 行

### 拆分策略（优先级排序）

1. **按职责拆分**（首选）：单一类职责过多时拆分为多个类
2. **按类型/接口拆分**：类型定义过多时按领域拆分
3. **按功能模块拆分**：服务类 → 主类 + 辅助类，子目录组织
4. **按层级拆分**：Provider → Provider + Helper + MessageHandler

### 拆分原则

✅ 拆分前思考职责边界  
✅ 使用 `index.ts` 统一导出  
✅ 每个文件清晰的 JSDoc 说明职责  
❌ 不机械拆分（破坏逻辑完整性）  
❌ 不过度拆分（单函数独立成文件）  
❌ 不循环依赖

## 注释规范

### 优先使用扩展生成

- **函数注释**: turbo-file-header 扩展（快捷键 `ctrl+alt+/`），补充函数作用说明
- **调试日志**: turbo-console-log 扩展（快捷键 `ctrl+alt+l`）

### 注释格式

返回值、参数用 `{type}` 或 `{auto}`

```typescript
/**
 * @description 生成文件内容
 * @return {string}
 * @param rules {ParsedRule[]}
 */
private generateFileContent(rules: ParsedRule[]): string {
  // ...
}
```

## 测试覆盖要求

### 单元测试（Vitest）- 目标 80%+

**必须覆盖模块:**
- GitManager（URL 验证、错误处理）
- MdcParser（有效/无效 MDC 解析）
- RulesManager（索引、搜索、冲突检测）
- FileGenerator（配置生成、验证）
- Adapters（格式转换）

**测试文件:** `src/test/unit/*.test.ts`

### 集成测试（Mocha）

**必须覆盖流程:**
- 命令流程（addSource、syncRules、searchRules）
- 端到端（添加源 → 同步 → 生成配置）
- UI 交互（TreeView 刷新、StatusBar 更新）

**测试文件:** `src/test/suite/*.spec.ts`

## 文档组织规范（强制）

完整规范见 `docs/00-documentation-system.md`（开发前必读）

### 目录结构

```
docs/
├── README.md                        # 文档索引
├── 00-documentation-system.md       # 文档组织规范（必读第一个）
├── 01-overall-design.md             # 整体架构设计
├── development/                     # 开发文档
│   ├── adapters/                    # 对应 src/adapters/
│   ├── commands/                    # 对应 src/commands/
│   ├── parsers/                     # 对应 src/parsers/
│   ├── providers/                   # 对应 src/providers/
│   ├── services/                    # 对应 src/services/
│   ├── utils/                       # 对应 src/utils/
│   ├── webview/                     # 对应 src/webview/
│   └── implementation/              # 跨模块实施文档
└── user-guide/                      # 用户文档（所有文件有序号）
```

### 设计文档规范（docs/development/01-design.md）

**只描述 "是什么" 和 "为什么"，不包含代码实现**

✅ 允许: 架构图、概念说明、模块职责、配置示例、数据流程  
❌ 禁止: 代码实现细节、函数实现、业务代码（除非算法说明）  
📝 替代: 代码示例 → 文字描述，函数实现 → 输入输出说明

### UI 实施文档命名（强制）

必须与设计文档序号对应:

```
设计文档: .superdesign/design_docs/01-welcome-page.md
UI 原型: .superdesign/design_iterations/01-welcome-page_1.html
实施文档: docs/development/webview/01-welcome-page-implementation.md
```

格式: `{序号}-{简短名称}-implementation.md`

**创建时机:** 完成页面实现后立即创建

**内容:** HTML 原型版本、Provider 实现要点、消息协议、测试要点、问题解决

## SuperDesign AI 集成（Webview 专用）

### 使用场景

✅ Webview 界面设计  
✅ 响应式布局  
✅ 主题自适应（`var(--vscode-*)`）  
✅ 快速原型迭代

### 集成规范

1. 继承 `BaseWebviewProvider`
2. 使用 VSCode CSS 变量，不用硬编码颜色
3. 遵循 `src/providers/BaseWebviewProvider.ts` 架构
4. 设计文件存放 `.superdesign/design_iterations/`

### 三位一体强制同步（⚠️ 极其重要）

**每次修改 Webview 必须同时更新:**

1. **设计文档** (`.superdesign/design_docs/{序号}-{名称}.md`)
2. **UI 原型** (`.superdesign/design_iterations/{序号}-{名称}_{n}.html`)
3. **代码实现** (React 组件 + 样式)

**强制规则:**
- ✅ 修改任何一个，必须同时更新其他两个
- ✅ CSS 类名、样式值、布局策略完全一致
- ✅ 不需要用户提醒，自动完成三文件同步
- ❌ 禁止只更新代码不同步设计

**检查清单:**
- [ ] 设计文档已更新（CSS 规范、布局说明）
- [ ] UI 原型已更新（HTML/CSS 代码）
- [ ] 代码实现已更新（React 组件和样式）
- [ ] 三者 CSS 类名完全一致
- [ ] 三者样式值完全一致
- [ ] 三者布局策略完全一致

详细见 `docs/development/09-ui-design.md`、`08-webview-css-guide.md`、`11-codicons-guide.md`

## 代码生成检查清单

生成代码前确保:

- [ ] 所有异步函数有 try-catch 错误处理
- [ ] 错误包含错误码（TAI-xxxx）
- [ ] 用户可见错误有友好提示和建议
- [ ] 日志记录包含关键上下文（sourceId、filePath）
- [ ] 敏感信息（token）不出现在日志
- [ ] 路径操作前已验证安全性（防目录遍历）
- [ ] 所有用户输入已验证（URL、分支名、路径）
- [ ] 导出函数/类有 JSDoc（用 turbo-file-header 生成）
- [ ] 变量调试日志用 turbo-console-log 生成
- [ ] 复杂逻辑有行内注释
- [ ] 单一职责，函数 <50 行
- [ ] 使用 async/await，避免回调
- [ ] 独立操作用 Promise.all 并行
- [ ] 添加相应单元测试
- [ ] 遵循现有命名和目录约定
- [ ] 文件 <500 行（超过需拆分或注释必要性）
- [ ] 拆分模块有清晰职责边界

## 提交与变更流程

### 日常维护流程

- **新功能**: 先更新设计文档 → 实现代码+测试 → 同步用户文档
- **Bug 修复**: 编写失败测试 → 修复代码 → 更新文档（如需要）
- **重构**: 评估影响 → 更新设计 → 重构实现 → 同步文档

### Commit 类型

feat, fix, style, perf, refactor, revert, test, docs, chore, workflow, ci, mod, wip, types

### 文档同步

- 设计-代码-文档必须一致
- 修改代码行为时，必须同步更新相关文档
- 新增功能时，先更新设计文档，再实现代码

## 命名规范

- **类/接口**: PascalCase（RuleSource、GitManager）
- **函数/方法**: camelCase（cloneRepository、parseRules）
- **常量**: UPPER_SNAKE_CASE（DEFAULT_CACHE_DIR）
- **私有成员**: `_cache`
- **文件**: PascalCase.ts（服务类）、camelCase.ts（工具函数）、types/ 下 camelCase.ts

## 导入顺序

Node 内置 → 第三方 → 内部别名(@/) → 相对路径

## 核心依赖

- **simple-git**: Git 操作
- **fs-extra**: 增强文件系统
- **gray-matter**: 解析 frontmatter
- **@ygqygq2/vscode-log**: 统一日志

## 核心数据结构

### RuleSource（规则源配置）

- 唯一标识（id，kebab-case）
- Git 仓库信息（gitUrl, branch, subPath）
- 启用状态和同步策略
- 认证配置（token/ssh/none）

### ParsedRule（解析后的规则）

- 规则标识和标题
- Markdown 内容（不含 frontmatter）
- 元数据（version, tags, priority, author）
- 来源信息（sourceId, filePath）

详见 `src/types/`

## 最重要

修改/修复代码后，务必确保设计、代码与文档一致，必要时修改 SuperDesign 的 HTML（简单修改）
<!-- END_RULE -->

<!-- END_SOURCE source="user-rules" -->
<!-- USER-RULES:END -->
<!-- TURBO-AI-RULES:END -->